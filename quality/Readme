
Содержимое каталога: 
    * qialityTable.txt сводная таблица по качеству на тестовом множестве для различных настроек
    * TrainTestAOI.zip Полигоны обучающего и тестового множеств а также тестовая область внутри которой производится поиск рубок
    * каталоги с номером коммита (например 00229878966616f81080b1e22465f6eef4ada977): 
        - номер коммита означает состояние кода для которого производились измерения (в репозитории https://earthengine.googlesource.com/users/kolesovdm/ALARM, является подмодулем данного репозитория)
        - внутри каталога хранятся:
            + сгенерированные обучающие и тестовые данные, использованные для настройки нейросети; 
            + тестовые данные для оценки качества
            + блокнот с ходом обучения нейросети




Оценка точности происходила по следующей схеме:
    1. Берутся тестовые полигоны, не используемые при обучении.
    2. Внутри каждого полигона строится прогноз рубка/не рубка на указанную в полигоне дату рубки (или случайную дату, если рубки не было).
    3. Рассчитывается средний квадрат разности ожидаемого значения и реального.
    4. Рассчитываются статистики по всем полигонам (один полигон - один пример, размер полигона не учитывается) и заносятся в таблицу.

